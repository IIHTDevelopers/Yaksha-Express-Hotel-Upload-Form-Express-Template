const express = require('express');
const router = express.Router();

// POST route to handle the form submission

// Get all hotels with filtering, sorting, and pagination options

module.exports = router;
